{
  "name": "yield-optimizer-contracts",
  "version": "1.0.0",
  "description": "YieldVault LayerZero V2 OApp contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:arb": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "peer:sepolia": "hardhat run scripts/setPeer.ts --network sepolia",
    "peer:arb": "hardhat run scripts/setPeer.ts --network arbitrumSepolia",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@layerzerolabs/oapp-evm": "^0.1.0",
    "@openzeppelin/contracts": "^5.0.2"
  }
}
