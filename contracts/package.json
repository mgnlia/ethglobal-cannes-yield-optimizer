{
  "name": "crossyield-contracts",
  "version": "1.0.0",
  "description": "CrossYield smart contracts — LayerZero V2 cross-chain yield optimizer",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat deploy --network sepolia",
    "deploy:arbitrum": "hardhat deploy --network arbitrumSepolia",
    "verify:sepolia": "hardhat verify --network sepolia",
    "lint": "solhint 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.22.0",
    "hardhat-deploy": "^0.12.0",
    "solhint": "^4.0.0",
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@layerzerolabs/lz-evm-oapp-v2": "^2.3.3",
    "@layerzerolabs/lz-evm-protocol-v2": "^2.3.3",
    "@openzeppelin/contracts": "^5.0.0"
  }
}
